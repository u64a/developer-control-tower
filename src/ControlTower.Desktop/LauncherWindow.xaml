<Window x:Class="ControlTower.Desktop.LauncherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="CenterOwner"
        Width="720"
        SizeToContent="Height"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <Border Background="{DynamicResource CardBackgroundBrush}"
            BorderBrush="{DynamicResource SecondaryBorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Margin="24"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1" />
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="44" ShadowDepth="12" Direction="270" Opacity="0.55" Color="#000000" />
        </Border.Effect>
        <Border.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.15" />
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="0.98" To="1" Duration="0:0:0.16" />
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="0.98" To="1" Duration="0:0:0.16" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
        <DockPanel LastChildFill="True">

            <!-- Search row -->
            <Border DockPanel.Dock="Top"
                    BorderBrush="{DynamicResource CardBorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="18,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               FontFamily="{StaticResource SymbolIconFont}"
                               Text="&#xE721;"
                               FontSize="18"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0" />
                    <Grid Grid.Column="1">
                        <TextBox x:Name="SearchBox"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 FontSize="18"
                                 VerticalContentAlignment="Center"
                                 Foreground="{DynamicResource PrimaryTextBrush}"
                                 CaretBrush="{DynamicResource AccentBrush}" />
                        <TextBlock x:Name="Watermark"
                                   Text="Search projects — Enter to open, Ctrl+Enter to expand"
                                   FontSize="18"
                                   IsHitTestVisible="False"
                                   VerticalAlignment="Center"
                                   Margin="2,0,0,0"
                                   Foreground="{DynamicResource SecondaryTextBrush}" />
                    </Grid>
                    <Border Grid.Column="2"
                            Background="{DynamicResource PanelSubtleBrush}"
                            CornerRadius="4"
                            Padding="7,2"
                            VerticalAlignment="Center">
                        <TextBlock Text="Esc" Style="{StaticResource CaptionTextStyle}" />
                    </Border>
                </Grid>
            </Border>

            <!-- Footer hint -->
            <Border DockPanel.Dock="Bottom"
                    Background="{DynamicResource StatusBackgroundBrush}"
                    BorderBrush="{DynamicResource CardBorderBrush}"
                    BorderThickness="0,1,0,0"
                    Padding="18,11">
                <TextBlock Style="{StaticResource CaptionTextStyle}">
                    <Run Text="&#x2191;&#x2193; navigate" />
                    <Run Text="   ·   " />
                    <Run Text="&#x21B5; open" />
                    <Run Text="   ·   " />
                    <Run Text="Shift+&#x21B5; local/remote" />
                    <Run Text="   ·   " />
                    <Run Text="Ctrl+&#x21B5; expand" />
                    <Run Text="   ·   " />
                    <Run Text="Ctrl+L library" />
                    <Run Text="   ·   " />
                    <Run Text="Esc" />
                </TextBlock>
            </Border>

            <!-- Results -->
            <ListBox x:Name="ResultsList"
                     ItemsSource="{Binding Projects}"
                     SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
                     MaxHeight="430"
                     BorderThickness="0"
                     Background="Transparent"
                     Margin="8"
                     HorizontalContentAlignment="Stretch"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="ResultsDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="4,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Ellipse Grid.Column="0"
                                     Style="{StaticResource StatusLampStyle}"
                                     Margin="2,0,12,0"
                                     AutomationProperties.Name="{Binding Project.RepoState, Converter={StaticResource RepoStateToWord}}" />
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Project.DisplayName}"
                                           FontWeight="SemiBold"
                                           FontSize="14"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Style="{StaticResource CaptionTextStyle}"
                                           TextTrimming="CharacterEllipsis"
                                           Margin="0,1,0,0">
                                    <Run Text="{Binding Project.WorkspaceMode}" />
                                    <Run Text="  ·  " />
                                    <Run Text="{Binding Project.Branch}" />
                                </TextBlock>
                            </StackPanel>
                            <ContentControl Grid.Column="2"
                                            VerticalAlignment="Center"
                                            Margin="12,0,4,0"
                                            Content="{Binding Project}"
                                            ContentTemplate="{StaticResource StatePillTemplate}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Border>
</Window>
