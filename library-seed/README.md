# Starter asset library

Developer Control Tower copies this seed into the user's writable configuration
root only when no library exists. Existing libraries and assets are never
overwritten.

This starter intentionally contains no third-party templates, icons, generated
documents, or customer-specific material.
